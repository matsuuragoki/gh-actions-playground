name: Auto Assign Author

on:
  push:
  pull_request_target:
    types: [opened]

jobs:
  auto-author-assign:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: technote-space/assign-author@v1
